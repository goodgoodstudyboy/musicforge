# v14.3.2 MusicForge Expression Binding 单遍扫描性能稳定化施工图

## 目标

v14.3.1 已压缩 call-effect component，但最终 Windows Quality CI 的
`v140.interface_application_boundary_smoke` 仍为 220-233 秒，超过未变更的
180 秒硬预算。v14.3.2 只处理 collector 的重复 AST 扫描，不扩业务范围。

## 实施

1. 用一次迭代扫描同时识别 uncertain、unknown、Any 和 qualified Any。
2. 同一 AST expression occurrence 在 assignment、call summary 和 visitor
   阶段复用同一个 immutable `FlowValue`，不重复执行等价数据流分析。
3. Annotation counting 改为单遍；quoted annotation 只缓存 parsed AST，
   在文件间共享 immutable syntax，binding/count 仍按当前 lexical scope 计算；
   potential-binding classification 使用同一单遍规则。
4. 保持 quoted annotation、Attribute base shadowing、Load/Store 与 schema 14
   call-effect 语义。
5. 保留 v14.2.1-v14.3.1 全部攻击回归和 100-annotation 三层 ratchet。
6. 新增 ADR-029 与 `v1432.expression_binding_single_pass_smoke`。
7. Explicit Any、layer、file、complexity、coverage、recovery 和 performance
   ceiling 全部禁止提高。

## 验收

- 全量 pytest、Ruff、mypy、compileall、doctor 通过。
- v14/latest/security/GA/full release-check 通过。
- Windows Quality 与 Nightly LTS 在最终 SHA 通过。
- Reviewer Package 固定 17 个安全 entry，独立 `--require-final` 通过。
- v14.3.1 保留 tag，并标记为 superseded/non-recommended。
